Sanity authoring rules (як НЕ зламати сайт)
🎯 Мета

Редактор може створювати контент, але не може зламати SEO, layout або UX.

Загальні правила

Кожен пост = 1 стаття = 1 H1

H1 — тільки в полі “Title”

У body НЕ використовувати H1 (тільки H2 / H3)

Поля Post
Title

Обовʼязковий

Використовується як:

H1 на сторінці

fallback для Meta title

Description

Обовʼязкова

120–160 символів — ідеально

Використовується:

у списках

як fallback для Meta description

Featured Image

Обовʼязкова

Alt text рекомендований

Використовується як:

hero image

fallback для OG image

Content (Rich text)

Дозволено:

Paragraph

H2 / H3

Lists

Images

Заборонено:

H1

inline HTML

порожні блоки

SEO block (опціонально)

Meta title → 50–60 символів

Meta description → 120–160 символів

Canonical → тільки якщо знаєш, що робиш

OG image → якщо потрібен окремий превʼю

❗ Якщо SEO блок порожній — сайт працює коректно (є fallbackʼи)

Мови

EN — обовʼязкова

ES / RU — бажано

Якщо перекладу немає → сайт використовує fallback
