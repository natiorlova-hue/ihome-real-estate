Sanity authoring rules (how NOT to break the site)
🎯 Purpose

Editors can safely create content without breaking SEO, layout, or UX.

General rules

One post = one article = one H1

H1 lives only in the “Title” field

Never use H1 inside the article body (H2 / H3 only)

Post fields
Title

Required

Used as:

Page H1

Fallback for Meta title

Description

Required

Ideal length: 120–160 characters

Used as:

Blog listing excerpt

Fallback for Meta description

Featured Image

Required

Alt text strongly recommended

Used as:

Article hero image

Fallback for Open Graph image

Content (Rich Text)

Allowed:

Paragraphs

H2 / H3

Lists

Images

Not allowed:

H1

Inline HTML

Empty blocks

SEO block (optional)

Meta title → 50–60 characters

Meta description → 120–160 characters

Canonical → only if you know why

OG image → use only if different from featured image

❗ If the SEO block is empty, the website still works correctly (safe fallbacks exist).

Languages

English (EN) — required

Spanish (ES), Russian (RU) — recommended

Missing translations automatically fall back to EN
